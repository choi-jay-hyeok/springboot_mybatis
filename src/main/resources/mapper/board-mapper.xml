<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
    <insert id="save" parameterType="board"> <!-- parameterType="board"는 mybatis-config 에서 alias 로 설정한 board, alias 안해주면 parameterType 에 매번 전체 경로를 써줘야 함-->
        insert into board_table(boardTitle, boardWriter, boardPass, boardContents)
            values(#{boardTitle}, #{boardWriter}, #{boardPass}, #{boardContents}) <!-- #{} 안에는 dto 필드 이름과 동일해야 함 -->
    </insert>
</mapper>